# MoronTown 使用说明

MoronTown 是一款基于 Three.js、TypeScript 和 Vite 的 3D 城市驾驶模拟游戏。游戏包含 1200×1200 有限城市、无尽模式、AI 竞速、车辆车库、AI 交通、GLB 行人模型、局域网多人联机与触屏控制，支持键盘和触屏两种操控方式。

## 运行方式

```bash
npm install
npm run dev
```

启动后浏览器打开 Vite 输出的本地地址（默认 `http://localhost:5173`）即可进入游戏。

```bash
npm run build      # 构建生产版本
npm run serve      # 生产模式：同时托管页面与多人 WebSocket（默认 8080 端口）
npm test           # 运行 Playwright 测试
```

## 键位说明

### 电脑操控

| 按键 | 功能 |
| --- | --- |
| `W` / `↑` | 油门加速 |
| `S` / `↓` | 刹车减速；停车后按住可倒车 |
| `A` / `←` | 向左转向 |
| `D` / `→` | 向右转向 |
| `Space` | 手刹，转弯时按住进入街机式漂移姿态（只改变车身姿态，不影响过弯物理） |
| `Esc` | 暂停 / 继续游戏 |
| `R` | 重置车辆 |
| `C` | 切换视角（追尾视角 / 引擎盖视角） |
| `M` | 静音 / 取消静音 |
| `E` | 在竞速任务点内打开/关闭竞速设置面板 |
| `鼠标右键拖动` | 第三人称视角下以车为中心环绕旋转视角，松开右键平滑复位 |
| `Enter` | 预留确认键，当前版本尚未绑定实际功能 |

补充说明：

- 自由漫游中按 `R`，车辆会回到城市中心。
- 竞速中按 `R`，车辆会重置到下一个检查点前方；比赛结束后重置无效。
- 竞速中倒计时结束前无法正常驾驶，倒计时结束后才能操作。

### 手机操控

在主菜单选择“手机操控”后，进入自由漫游或竞速时会显示触屏控制：

- 左侧虚拟摇杆：控制前进、后退与左右转向。
- `油门`：加速。
- `刹车`：减速；停车后按住可倒车。
- `手刹`：低速甩尾。
- `视角`：切换追尾视角 / 引擎盖视角。
- HUD 上的 `暂停` 按钮：暂停游戏。

## 菜单与界面功能

### 主菜单

主菜单包含以下功能：

- `自由漫游`：进入开放城市自由驾驶，场景中会生成 AI 交通车辆和行人，城市中还有 3 个竞速任务触发点（详见下文）。
- `无尽模式`：进入按区块流式生成的无尽道路，可一直向前驾驶，河流与桥梁会随区块持续出现。
- `多人游戏`：连接同一局域网内的 WebSocket 房间服务，查看房间列表、创建房间或加入好友房间。
- `竞速模式`：进入竞速设置界面。
- `车库`：进入车库选择车辆和颜色。
- `设置`：调整背景音乐音量、游戏音量、声音开关、操控方式、交通密度和画质（自动/高/中/低）。
- `声音：开 / 声音：关`：切换游戏声音。
- `电脑操控 / 手机操控`：切换键盘或触屏控制方式，选择会自动保存。

### 多人模式

多人模式面向同一局域网内的玩家，需要先由一人启动游戏服务：

```bash
npm run dev        # 开发模式，WebSocket 与页面共用 5173 端口
npm run serve      # 生产模式，页面与 WebSocket 共用 8080 端口
```

- 进入“多人游戏”后会自动分配随机中文用户名。
- 可以创建房间（填写房间名称）或从房间列表加入好友房间，每房最多 8 人。
- 房主在大厅点击“开始游戏”后，所有玩家进入同一张有限地图自由漫游，其他玩家的车辆会实时同步显示。
- 返回主菜单会自动退出当前房间。

### 竞速模式

竞速为“城市环路 · 3 圈 · 4 台车”的 AI 竞速比赛：

- 难度：`简单`、`普通`、`困难`。难度越高，AI 车辆的极速、过弯能力和追车调整越强。
- `开始比赛`：按当前难度开始比赛。
- `返回`：返回主菜单。

比赛开始前有 3 秒倒计时，倒计时结束后开始计时。比赛中需要按顺序通过检查点，每完成一圈记录一次圈速，完成 3 圈后显示最终成绩。

### 车库

车库提供 6 款车辆：

- 城市轿车（初始座驾）
- 运动轿跑（🪙 4000）
- 越野 SUV（🪙 1800）
- 经典皮卡（🪙 900）
- 城市出租车（🪙 500）
- 警用拦截车（🪙 2800）

每辆车展示 `极速`、`加速`、`操控`、`制动` 四项属性，并显示随转速变化的扭矩曲线与各档位齿比。**除城市轿车外的车辆都需要用金币解锁**：选中未拥有的车辆后点击 `购买并装备` 即可解锁并立即使用（金币不足时按钮会禁用）。选中已拥有的车辆后可选择车身颜色，点击 `使用此车辆` 保存选择并返回主菜单。

### 金币经济

金币是游戏内货币，用于解锁车辆，获取途径：

- **竞速结算**：第 1 名 🪙300、第 2 名 🪙200、第 3 名 🪙150、其余名次 🪙100，未完赛 🪙60。
- **行驶里程**：每行驶 1 km 获得 🪙2。
- **行人击杀**：每撞倒 1 名行人获得 🪙5。
- **每日签到**：每天可领一次，第 n 天领 n×50（封顶 300），连续签到断签后重新累计。
- **每日任务**：每天 3 个随机任务，完成后手动领取奖励。
- **成就**：达成条件自动解锁并发放奖励金币。

### 生涯成就页

主菜单进入 `生涯成就`，包含：

- **每日签到**：显示当前连续签到天数与今日可领奖励，点击领取。
- **今日任务**：每天 3 个任务（城市巡游、竞速挑战、行人克星/无尽之旅二选一），进度条实时显示，完成后点 `领取` 到账。
- **成就**：14 个成就（里程、竞速、冠军、极速、击杀、签到、赚取等），达成自动解锁并发放金币。
- **生涯统计**：累计里程、无尽里程、竞速场次、夺冠次数、极速纪录、驾驶时长、累计赚取、行人击杀。

金币与生涯数据保存在浏览器本地，跨会话保留。

### 竞速结算与分享

比赛完成后显示最终名次、总用时与最佳圈速，同时显示本场获得的金币奖励；若刷新了该赛道当前难度的最佳圈速会提示 `刷新赛道纪录`。可点击 `分享成绩` 调起系统分享（不支持时复制到剪贴板）。

### HUD 信息

自由漫游与竞速中的 HUD 包含：

- 实时速度（km/h）。
- 当前金币数（左下角，行人击杀面板上方；手机端位于右上角击杀计数上方）。
- 竞速信息：圈数、当前名次、比赛用时、最佳圈速。
- 小地图：显示玩家位置，竞速时还会显示其他赛车位置。
- `暂停` 按钮：暂停当前游戏。

### 暂停菜单

游戏中按 `Esc` 或点击 HUD 的 `暂停` 按钮会打开暂停菜单：

- `交通密度`：`少`、`适量`、`大`，控制自由漫游中 AI 交通与行人的数量。
- `继续`：返回游戏。
- `重新开始`：重新开始当前模式（自由漫游重置车辆，竞速重新开局）。
- `返回主菜单`：退出当前游戏，回到主菜单。

### 比赛结算

比赛完成后显示：

- 最终名次（第一名、第二名等，未完赛会显示未完成）。
- 总用时与最佳圈速。
- `再来一局`：以当前难度重新开始比赛。
- `返回主菜单`：返回主菜单。

## 玩法提示

- 自由漫游中有 AI 车辆和行人，碰撞会让车辆减速并播放撞击音效。
- **NPC 交通更真实**：AI 车辆沿样条平滑转向（弯道自动减速），加减速与转向均有过渡；玩家撞击 NPC 后，NPC 会按动量自然偏移，随后平滑回归车道继续行驶，全程无瞬移、无跳变。
- 竞速模式中 AI 车辆会自动沿赛道行驶并避开玩家，可以通过难度调节强度。
- 车辆选择、车身颜色、声音开关、操控方式和交通密度会保存在浏览器本地，下次打开自动生效。
- 车辆与行人模型来自免费/CC0 许可的公开资源，许可信息见 `public/models/LICENSES.md`；模型加载失败时游戏会自动回退到程序化模型。
- 部分浏览器要求用户先点击页面后才能播放声音，首次进入游戏并点击“自由漫游”或“开始比赛”后音频即可正常使用。

## 竞速任务触发点

自由漫游的城市地图中有 3 个发光任务点（蓝白色呼吸圆环 + 中央光柱，远距离可见），分别对应 3 张竞速地图：

| 任务点 | 位置 | 对应竞速地图 |
| --- | --- | --- |
| 城市环路 | 城市中西部 | 城市环路 |
| 城市巡回 | 城市东部 | 城市巡回 |
| 山地纵贯 | 山地入口 | 山地纵贯 |

流程：驶入圆环 → 屏幕下方出现「按 E 开始竞速」提示 → 按 E 打开竞速设置面板（游戏暂停）→ 选择圈数（1–5 圈）与对手数量（1–7 人）→ 点「开始比赛」进入对应竞速；再按 E 或点「取消」关闭面板恢复自由漫游。比赛结束后点「返回自由漫游」会回到进入任务点前的位置。

任务点由**发光圆柱墙 + 顶部/地面圆环 + 中央光柱 + 悬浮信标**组成，常亮蓝色，远距离即可辨认，可在自由漫游中驾车前往。

任务点位置与默认参数统一在 `src/core/Constants.ts` 的 `TASK_POINTS` 中配置（数据驱动）。
