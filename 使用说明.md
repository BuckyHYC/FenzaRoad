# FenzaRoad 使用说明

FenzaRoad 是一款基于 Three.js、TypeScript 和 Vite 的 3D 城市驾驶模拟游戏。游戏包含自由漫游、AI 竞速、车辆车库、AI 交通、行人与触屏控制，支持键盘和触屏两种操控方式。

## 运行方式

```bash
npm install
npm run dev
```

启动后浏览器打开 Vite 输出的本地地址（默认 `http://localhost:5173`）即可进入游戏。

```bash
npm run build      # 构建生产版本
npm test           # 运行 Playwright 测试
```

## 键位说明

### 电脑操控

| 按键 | 功能 |
| --- | --- |
| `W` / `↑` | 油门加速 |
| `S` / `↓` | 刹车减速；停车后按住可倒车 |
| `A` / `←` | 向左转向 |
| `D` / `→` | 向右转向 |
| `Space` | 手刹，可在低速时甩尾过弯 |
| `Esc` | 暂停 / 继续游戏 |
| `R` | 重置车辆 |
| `C` | 切换视角（追尾视角 / 引擎盖视角） |
| `M` | 静音 / 取消静音 |
| `Enter` | 预留确认键，当前版本尚未绑定实际功能 |

补充说明：

- 自由漫游中按 `R`，车辆会回到城市中心。
- 竞速中按 `R`，车辆会重置到下一个检查点前方；比赛结束后重置无效。
- 竞速中倒计时结束前无法正常驾驶，倒计时结束后才能操作。

### 手机操控

在主菜单选择“手机操控”后，进入自由漫游或竞速时会显示触屏控制：

- 左侧虚拟摇杆：控制前进、后退与左右转向。
- `油门`：加速。
- `刹车`：减速；停车后按住可倒车。
- `手刹`：低速甩尾。
- HUD 上的 `暂停` 按钮：暂停游戏。

## 菜单与界面功能

### 主菜单

主菜单包含以下功能：

- `自由漫游`：进入开放城市自由驾驶，场景中会生成 AI 交通车辆和行人。
- `竞速模式`：进入竞速设置界面。
- `车库`：进入车库选择车辆和颜色。
- `声音：开 / 声音：关`：切换游戏声音。
- `电脑操控 / 手机操控`：切换键盘或触屏控制方式，选择会自动保存。

### 竞速模式

竞速为“城市环路 · 3 圈 · 4 台车”的 AI 竞速比赛：

- 难度：`简单`、`普通`、`困难`。难度越高，AI 车辆的极速、过弯能力和追车调整越强。
- `开始比赛`：按当前难度开始比赛。
- `返回`：返回主菜单。

比赛开始前有 3 秒倒计时，倒计时结束后开始计时。比赛中需要按顺序通过检查点，每完成一圈记录一次圈速，完成 3 圈后显示最终成绩。

### 车库

车库提供 6 款车辆：

- 城市轿车
- 运动轿跑
- 越野 SUV
- 经典皮卡
- 城市出租车
- 警用拦截车

每辆车展示 `极速`、`加速`、`操控`、`制动` 四项属性。选中车辆后可选择车身颜色，点击 `使用此车辆` 保存选择并返回主菜单。

### HUD 信息

自由漫游与竞速中的 HUD 包含：

- 实时速度（km/h）。
- 竞速信息：圈数、当前名次、比赛用时、最佳圈速。
- 小地图：显示玩家位置，竞速时还会显示其他赛车位置。
- `暂停` 按钮：暂停当前游戏。

### 暂停菜单

游戏中按 `Esc` 或点击 HUD 的 `暂停` 按钮会打开暂停菜单：

- `交通密度`：`少`、`适量`、`大`，控制自由漫游中 AI 交通与行人的数量。
- `继续`：返回游戏。
- `重新开始`：重新开始当前模式（自由漫游重置车辆，竞速重新开局）。
- `返回主菜单`：退出当前游戏，回到主菜单。

### 比赛结算

比赛完成后显示：

- 最终名次（第一名、第二名等，未完赛会显示未完成）。
- 总用时与最佳圈速。
- `再来一局`：以当前难度重新开始比赛。
- `返回主菜单`：返回主菜单。

## 玩法提示

- 自由漫游中有 AI 车辆和行人，碰撞会让车辆减速并播放撞击音效。
- 竞速模式中 AI 车辆会自动沿赛道行驶并避开玩家，可以通过难度调节强度。
- 车辆选择、车身颜色、声音开关、操控方式和交通密度会保存在浏览器本地，下次打开自动生效。
- 部分浏览器要求用户先点击页面后才能播放声音，首次进入游戏并点击“自由漫游”或“开始比赛”后音频即可正常使用。
